$(document).ready(function () {

    var gifCount = 0;

    $("add-gif-button").on('click', function (event) {
        event.preventDefault();

        //gets the gif that we need to search and returns it to the gifToReturn Variable
        var gifToReturn = $("#searchThisGif").val().trim();

        //creates a p that will hold all the gif's we create
        var createParagraph = $("<p>");
        //adds an 
        createParagraph.attr("id", item)
    });


})